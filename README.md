# compileC
                                                              
This is a small compiler to learn about the stages required in an actual compiler. Our compiler is able to parse C code and turn it into somewhat working ASM, but it has optimization issues when generating the output. 
